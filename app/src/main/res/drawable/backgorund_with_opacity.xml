<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Capa de la imagen de fondo -->
    <item android:drawable="@drawable/imagen_background" />

    <!-- Capa del color de fondo semitransparente -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#80000000" /> <!-- Cambia el nivel de opacidad ajustando el valor hexadecimal -->
        </shape>
    </item>
</layer-list>
